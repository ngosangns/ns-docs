# Concepts

* [SKILL](.agents/skills/mdvdb-index-vault/SKILL.md) - Ingest or re-index markdown (.md) files into the mdvdb vector database. Checks cost with mdvdb info, previews index/embedding work, then ingests and reports Formula/Lookup/Rollup materialization diagnostics. Must be explicitly invoked because it modifies the index and may safely update module-owned computed frontmatter.
